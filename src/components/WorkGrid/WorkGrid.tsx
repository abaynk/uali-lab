import React from "react";

type Props = {
  children: any;
};

export const WorkGrid = ({ children }: Props) => {
  return (
    <div className="WorkGrid_work-grid__0043M page_work-grid__VoMxE">
      {children}
    </div>
  );
};
